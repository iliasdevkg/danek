import { notFound } from "next/navigation";

/**
 * Любой неизвестный адрес внутри витрины.
 *
 * Без этого маршрута `not-found.tsx` показывался бы только там, где страница
 * сама позвала `notFound()` — например, на новости с удалённым слагом. А самый
 * частый случай, ради которого он и сделан, до него не доезжал: на
 * «/ru/старая-ссылка» Next отдавал свою служебную страницу 404, без шапки,
 * подвала и русского текста.
 *
 * Ловушка стоит последней в порядке подбора маршрутов, поэтому ни один
 * существующий раздел она не перехватывает.
 */
export default function SiteCatchAll() {
  notFound();
}
